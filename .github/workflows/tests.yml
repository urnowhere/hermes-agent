name: Tests

on:
  push:
    branches: [main]
  pull_request:

# Cancel in-progress runs for the same PR/branch
concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies (Linux)
        if: runner.os != 'Windows'
        run: sudo apt-get update && sudo apt-get install -y ripgrep

      - name: Install system dependencies (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          choco install ripgrep -y

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install dependencies (Linux)
        if: runner.os != 'Windows'
        run: |
          uv venv .venv --python 3.11
          source .venv/bin/activate
          uv pip install -e ".[all,dev]"

      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          uv venv .venv --python 3.11
          .\.venv\Scripts\Activate.ps1
          uv pip install -e ".[all,dev]"

      - name: Run tests (Linux)
        if: runner.os != 'Windows'
        run: |
          source .venv/bin/activate
          python -m pytest tests/scripts/test_install_ps1.py tests/scripts/test_ci_workflows.py tests/hermes_cli/test_profiles.py tests/tools/test_memory_tool.py tests/tools/test_local_env_blocklist.py tests/tools/test_local_env_windows.py -q -o "addopts="
        env:
          # Ensure tests don't accidentally call real APIs
          OPENROUTER_API_KEY: ""
          OPENAI_API_KEY: ""
          NOUS_API_KEY: ""

      - name: Run tests (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          .\.venv\Scripts\Activate.ps1
          python -m pytest tests/scripts/test_install_ps1.py tests/scripts/test_ci_workflows.py tests/hermes_cli/test_profiles.py tests/tools/test_memory_tool.py tests/tools/test_local_env_blocklist.py tests/tools/test_local_env_windows.py -q -o "addopts="
        env:
          OPENROUTER_API_KEY: ""
          OPENAI_API_KEY: ""
          NOUS_API_KEY: ""

  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Python 3.11
        run: uv python install 3.11

      - name: Install dependencies
        run: |
          uv venv .venv --python 3.11
          source .venv/bin/activate
          uv pip install -e ".[all,dev]"

      - name: Run e2e tests
        run: |
          source .venv/bin/activate
          python -m pytest tests/e2e/ -v --tb=short
        env:
          OPENROUTER_API_KEY: ""
          OPENAI_API_KEY: ""
          NOUS_API_KEY: ""
