## Status Ledger / Progress Doc（状态台账 / 进度文档）

**Task Contract Snapshot (合同快照)**  
- 目标：在仓库根目录建立一份可长期维护的 `AGENTS.md` 系统总图，并完成最小必要的目录结构初始化，使后续 Agent 能按“渐进式信息披露”获取上下文、定位规则与进入路径，而不是一次性吞入全部背景。  
- 范围边界：本轮只处理根目录 `AGENTS.md` 的系统总图职责、读取顺序、信息分层原则与最小目录结构初始化；不把 `AGENTS.md` 写成总说明书或培训手册；不一次性填满所有子目录细节；不扩展到 Hook、自动触发器、运行时守卫或完整批次 1 全量体系；不顺手重写现有 skills、批次 0 骨架或大量历史文档。  
- 完成标准：根目录 `AGENTS.md` 已正式建立或重构并可回读；其内容明确“它是地图，不是仓库全量说明书”；最小目录结构已初始化且与文档入口一致；后续 Agent 能按 `AGENTS.md` 的指引逐层进入，不会被一次性塞爆上下文；不存在明显的过度初始化或地图与真实路径不一致问题。  

**Current State (当前状态)**  
- 当前停点：`AGENTS.md` 与 `docs/agents/` 四个核心文件已完成 v2 主控精修、落盘与回读；本任务的验证链也已回写为最终版。  
- 已完成：已重构根 `AGENTS.md` 为地图型入口；已创建 `docs/agents/README.md`、`architecture.md`、`workflows.md`、`guardrails.md`；已删除 workflow 文档中的非法缩水模板，改为强制引用批次 0 标准骨架；已完成行数核查、存在性核查与目录洁净度核查。  
- 未完成 / 当前阻塞：本轮合同范围内无剩余阻断项。  
- 当前判断：可收口  

**Evidence Logged (证据登记)**  
- 已有证据：`AGENTS.md` 已回读，确认其为地图型入口；`wc -l AGENTS.md` 输出为 `82 AGENTS.md`；`docs/agents/` 四个文件已通过搜索与回读验证存在；未发现计划外新增目录；`workflows.md` 已明确改为引用批次 0 标准骨架而非自造模板。  
- 证据对应结论：本轮最小目录结构方案已真实落地且没有过度初始化；新版 `AGENTS.md` 达成了渐进式信息披露目标；二级文档职责边界清楚，没有再次串位。  
- 证据缺口：本轮合同范围内无关键证据缺口。  

**Next Handoff (下一步 / 接管指令)**  
- 接手后第一步：若进入批次 1 下一任务，先从根 `AGENTS.md` 读取地图，再按任务类型下钻；不要把根地图重新写成厚手册。  
- 立即核查：后续新增 Agent 文档时，先核对是否真有使用需求，再决定是否扩目录；优先补现有 4 个二级文档，不要预建空壳。  
- 若受阻先排查：如果未来 `AGENTS.md` 开始膨胀、二级文档职责串位、或有人再次尝试在文档里发明缩水模板，立即按批次 0 的 skill-change-loop 打回。