import type { SessionSummary } from '../lib/types';

export const initialSessions: SessionSummary[] = [
  { sessionId: 'current', title: 'Current Hermes session' },
  { sessionId: 'planning', title: 'Hermes Web Console planning' }
];
