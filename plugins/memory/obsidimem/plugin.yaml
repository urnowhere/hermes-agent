name: obsidimem
version: 1.0.0
description: "obsidi-mem memory — hybrid pgvector+BM25 search, peer cards, session summaries, and synthesized answers."
pip_dependencies:
  - httpx
hooks:
  - on_session_switch
  - on_memory_write
  - on_pre_compress
  - on_session_end
  - on_delegation
